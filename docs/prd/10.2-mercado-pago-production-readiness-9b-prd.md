# PRD — 10.2 Mercado Pago Production Readiness & 9B Preparation

**Status:** planned.
**Aggregate branch:** `feature/10.2`.
**Objective:** make Mercado Pago safe for production payments and prepare it as a reliable financial source for 9B Marketplace Settlement.

## 1. Problem

LedgerFlow already has a Mercado Pago payment foundation, but the provider is not yet production-ready end to end. OAuth and basic payment creation exist, while webhook processing, automatic refresh, cancel/refund, E2E validation and settlement-read readiness are incomplete.

Starting 9B settlement before closing those gaps would force reconciliation to rely on partial provider state and assumptions.

## 2. Expected outcome

```text
Mercado Pago OAuth connection
→ encrypted tenant credentials
→ automatic refresh lifecycle
→ PIX/boleto payment create/fetch
→ webhook inbox/outbox
→ status sync and notifications
→ production E2E evidence
→ financial-read readiness
→ 9B settlement ingestion/matching
```

## 3. Scope

### Mercado Pago payment readiness

- Current-state hardening and provider slug normalization.
- Automatic backend-only token refresh with lock/versioning.
- Webhook endpoint, signature validation, inbox/outbox and async processing.
- Payment status sync from provider webhook/fetch data.
- Cancel/refund support where allowed by provider and internal state.
- Production E2E payment runbook and validation evidence.

### 9B preparation

- Reuse `GatewayConfiguration` short-term for settlement-read readiness.
- Separate payment gateway behavior from financial-read/settlement services.
- Add financial health/scope checks.
- Create financial accounts/opening balance.
- Ingest Mercado Pago financial facts into normalized settlement events.
- Match Mercado Livre orders/payments with Mercado Pago financial events.

## 4. Out of scope

- Full card payment support until a secure card architecture is explicitly designed.
- XLSX export in the first settlement slice; CSV async is the accepted first format.
- Official accounting. Cash and P&L views are operational management views.
- Consuming Mercado Pago directly from n8n. n8n must consume sanitized LedgerFlow outbound events only.
- Creating a separate `FinancialIntegrationConnection` in the first iteration unless `GatewayConfiguration` becomes insufficient.

## 5. Personas and permissions

| Persona        | Need                              | Guardrail                                 |
| -------------- | --------------------------------- | ----------------------------------------- |
| Tenant admin   | Connect/reconnect Mercado Pago    | OAuth state is tenant/user/provider-bound |
| Finance user   | See payment/settlement health     | RBAC + capability + tenant scope          |
| Operator       | Understand payment status         | Sanitized status, no provider secrets     |
| Platform admin | Diagnose failures                 | Sanitized health and logs only            |
| Automation/n8n | Receive settlement/payment events | HMAC, idempotency and allowlisted payload |

## 6. Functional requirements

### RF-MP-1 — OAuth and connection feedback

The UI must show accurate success/error feedback after Mercado Pago OAuth callback regardless of provider slug shape in the query string.

Status MP-0: implemented with frontend provider normalization for `mercado-pago`, `mercado_pago` and `MERCADO_PAGO`.

### RF-MP-2 — Token refresh lifecycle

Provider operations must request a valid access token from a backend credential manager. Expired tokens refresh atomically; invalid/revoked refresh tokens put the connection in reauth-required/degraded state.

Status MP-1: implemented for Mercado Pago payment operations through `MercadoPagoCredentialManager`; invalid refresh marks `REAUTH_REQUIRED` and emits a permission-aware notification.

### RF-MP-3 — Webhook intake

Mercado Pago webhooks must be validated, persisted idempotently, processed asynchronously and safely mapped to tenant/provider resources.

Status MP-2: implemented as webhook intake foundation. LedgerFlow accepts `POST /webhooks/mercado-pago`, validates the Mercado Pago signature policy, persists sanitized inbox rows idempotently, and queues the existing generic webhook processing command. Payment status transitions remain MP-3/RF-MP-4 scope.

### RF-MP-4 — Payment status sync

Webhook/fetch processing must update internal `Payment` status idempotently, without terminal-state regressions, and with audit/notifications for meaningful transitions.

Status MP-3: implemented for Mercado Pago PIX/boleto payment records. The worker fetches provider payment details with a valid tenant token, maps approved/pending/rejected/cancelled/refunded safely, blocks terminal regressions, records chargeback-like statuses as payment events without changing the local status, and emits authorized `mercado_pago.payment_status_updated` notifications only on meaningful transitions.

### RF-MP-5 — Cancel/refund

Authorized users can cancel/refund Mercado Pago payments only when internal state and provider policy allow it. Invalid operations return translated user-friendly errors.

Status MP-4: implemented for Mercado Pago payment operations through the existing generic payment endpoints. Cancel/refund calls run through the provider before local mutation, use deterministic idempotency keys, require a refund reason, persist provider status/refund identifiers in sanitized event/audit metadata, and return idempotently when the target status is already complete locally.

### RF-MP-5A — Payment E2E operational validation

Mercado Pago payment readiness must be validated through a repeatable operational runbook before settlement-read flows are enabled.

Status MP-5: runbook `docs/runbooks/mercado-pago-payment-e2e.md` defines the production/staging evidence path for OAuth connection, PIX creation, Mercado Pago webhook signature/inbox, status sync, internal notification, outbound n8n delivery/dedupe, refresh token lifecycle, reauth diagnosis and boleto provider limitations. Real PIX/boleto evidence must be captured in the target environment; repository tests cover the deterministic code paths but do not replace provider evidence.

### RF-MP-6 — Financial readiness

LedgerFlow can determine whether a Mercado Pago connection is payment-only or settlement-readable based on scopes, token health and financial-read capability.

Status MP-6: implemented as derived readiness on gateway connection responses. Mercado Pago connections now expose whether they are payment-only or settlement-ready, required/granted/missing financial scopes, health/reauth blockers and the safe reason to show in UI. Future settlement reads must use `MercadoPagoFinancialReadinessService.assertSettlementReadable` before provider I/O. No new tenant token storage or settlement ingestion was added.

### RF-9B-1 — Financial accounts

Authorized users can create operational financial accounts and opening balances. Cash ledger movements are append-only and audited.

Status 9B-1: implemented for Mercado Pago operational accounts. The account must be linked to a settlement-ready Mercado Pago gateway connection, opening balance is stored in minor units, `CashLedgerEntry` is append-only, manual corrections require reason/notes, and `/marketplace-settlement` exposes the translated UI for authorized tenants.

### RF-9B-2 — Financial ingestion

Mercado Pago financial data can be fetched by period, normalized, sanitized and upserted as idempotent settlement events.

Status 9B-2: implemented for manual Mercado Pago financial ingestion by period. LedgerFlow fetches payment rows through a dedicated financial-read service, normalizes gross/fee/net/release/refund facts into `ProviderSettlementEvent`, scopes events to the selected `OperationalFinancialAccount`, emits `marketplace_settlement.event_received` only for newly-created rows, and exposes imported events/totals in the translated `/marketplace-settlement` UI. Scheduled sync and ML x MP matching remain future scope.

### RF-9B-3 — ML x MP matching

Settlement matching prioritizes exact provider identifiers. Amount/date matches are candidates only and require manual review.

Status 9B-3: implemented for Mercado Livre x Mercado Pago matching foundations. `OrderFinancialFact.externalOrderId` now stores the Mercado Livre order id structurally and `ReconciliationCase.orderId` is linked to `InternalOrder`. Matching order is provider payment id, payment external/reference, Mercado Livre external order id, then amount/date candidates. Exact ML order id matches can reconcile when amount/currency align; amount/date candidates become `PENDING` manual-review cases and never final auto-reconciliation.

### RF-9B-4 — Cash position and operational P&L

Marketplace settlement users can view released, pending, blocked, refunded and payout cash buckets plus an operational P&L built from imported settlement facts and matched Mercado Livre order facts.

Status 9B-4: implemented as an operational dashboard on `/marketplace-settlement`. `GET /marketplace-settlement/financial-accounts/:id/dashboard` returns cash position buckets derived from `ProviderSettlementEvent` and account balances, plus operational P&L with net revenue, fees, shipping, refunds, COGS, gross margin and matched order count. The response and UI include the required disclaimer that this is a management view, not official accounting. Payout stays zero until real payout events are ingested.

### RF-9B-5 — Settlement fine mesh

Marketplace settlement users can review case evidence, timeline and controlled reason codes before recording manual decisions.

Status 9B-5: implemented on the existing reconciliation case foundation. `GET /reconciliation/cases/reason-codes` exposes action-specific reason codes with translation keys and comment requirements. `GET /reconciliation/cases/:id/timeline` returns sanitized evidence for settlement event, linked payment/order and append-only decisions. The web review modal displays evidence/timeline and only offers valid localized reason codes. Manual resolve, ignore, reopen, comment and manual-match decisions remain append-only, audited, tenant-scoped and guarded by reconciliation permissions/capabilities.

### RF-9B-6 — Settlement notifications and n8n outbound

Authorized users and automations can receive sanitized settlement alerts without consuming raw Mercado Pago payloads directly.

Status 9B-6: implemented through the existing Notification Center and outbound webhook pipeline. `marketplace_settlement.event_received` and `cash_position.unexplained_difference` are registered permission-aware event contracts. Mercado Pago financial sync emits idempotent sanitized notifications only for newly-created settlement facts. Active notification webhook subscriptions can include those event types and receive the existing HMAC-signed payload with `Idempotency-Key`, delivery id and retry/DLQ semantics. n8n remains a consumer of LedgerFlow normalized events, not a direct provider webhook processor.

### RF-9B-7 — Exports, observability and final hardening

Finance users and operators can export settlement evidence, inspect a safe health snapshot and follow a repeatable operational checklist.

Status 9B-7: implemented. `MARKETPLACE_SETTLEMENT_EVENTS` joins the existing async CSV export pipeline, with streaming generation and tenant/account/provider/date filters. `/exports` exposes reconciliation and marketplace settlement CSV reports with localized labels. `GET /health/settlement` returns only sanitized aggregate counters for settlement events, open divergences, webhook DLQ and failed exports. The operational runbook `docs/runbooks/9B-marketplace-settlement-operational-readiness.md` documents evidence capture, n8n validation, export smoke tests, health checks, security checklist and rollback boundaries.

## 7. UX and design system

- Preserve the current dark enterprise visual language, layout rhythm and design tokens.
- Reuse existing components such as page headers, cards, tables, modals, badges, empty/error states, buttons and toasts.
- Use localized messages and status labels from `pt-BR` and `en-US`.
- Make reauth/degraded states explicit without exposing technical secrets.
- Long-running syncs must show immediate feedback and prevent duplicate submissions.
- Financial dashboards must include the disclaimer: operational management view; not official accounting.

## 8. Success metrics

- Zero token/secret exposure in UI, logs, audit, queues or notifications.
- Zero webhook cross-tenant attachment.
- Duplicate webhooks do not duplicate status transitions, audit or notifications.
- Expired tokens refresh without user reconnection during normal operation.
- Mercado Pago PIX E2E path has documented evidence before 9B ingestion starts.
- Settlement events are idempotent and searchable by provider identifiers.

## 9. Definition of Done

- Focused tests for token refresh, webhooks, tenant isolation, RBAC and idempotency.
- API build/lint and web type-check/build for touched areas.
- Prisma migrations validated when schema changes.
- Swagger/Redoc updated for REST contracts.
- AsyncAPI updated for implemented events.
- README, SDD, backlog, sprint plan, UI docs and runbooks updated with each sprint.
- pt-BR/en-US translations included for every user-facing string.
