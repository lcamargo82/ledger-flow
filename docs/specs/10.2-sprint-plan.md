# Spec — Plano de Sprints 10.2

**Status:** 10.2.0A–10.2.3 implementadas. Uma branch dedicada por sprint; merge na branch agregadora antes da integração final.

**Addendum:** Mercado Livre 10.1.11 foi integrado na branch agregadora `feature/10.2`. O próximo pacote planejado dentro da mesma agregadora é Mercado Pago Production Readiness & 9B Preparation, descrito em `docs/specs/10.2-mercado-pago-production-readiness-9b-plan.md`.

## Dependências

```text
10.1.10 operacional
→ 10.2.0A internal notifications
→ 10.2.0B outbound delivery
→ 10.2.1 inventory foundation
→ 10.2.2 transfers
→ 10.2.3 cycle counts
→ 10.1.11 ML enrichment
```

10.2.0B pode avançar em paralelo com 10.2.1 após congelar o contrato `NotificationEvent`, mas o caminho recomendado é sequencial.

## 10.2.0A — Notification Center interno

- Schema event/recipient, registry de categorias e audiência RBAC/capability.
- APIs paginadas, unread count, read/read-all/dismiss.
- Sino, versão e `/notifications`.
- Producers mínimos: falha de canal e divergência de conciliação, sob feature flag.
- Testes cross-tenant, permissão revogada, duplicata e paginação.
- Swagger/Redoc, i18n, README/UI docs e ADR 0036.

**Aceite:** contador/lista convergem; usuário vê apenas conteúdo autorizado; badge/footer funciona expandido e colapsado.

## 10.2.0B — Outbound webhooks para automação

- Subscription/delivery, segredo cifrado, assinatura HMAC e antireplay.
- Dispatcher Outbox/worker, retry/backoff/DLQ/replay.
- Endpoint de teste sem PII e métricas de entrega.
- AsyncAPI e runbook de rotação/diagnóstico.

**Aceite:** n8n valida assinatura, deduplica por idempotency key e indisponibilidade não perde nem duplica semanticamente o evento.

## 10.2.1 — Advanced Inventory Foundation

- Capabilities/permissões, seeds idempotentes e reason-code registry.
- Contratos/repositories e feature flags para transfers/cycle counts.
- Rotas/menu somente quando a feature estiver habilitada; placeholder não pode aparentar funcionalidade concluída.
- Documentar migrations, APIs, estados e componentes.

**Aceite:** nenhuma alteração de saldo; acesso negado no backend e frontend quando entitlement faltar.

## 10.2.2 — Transferências

- **Status:** implementada nesta branch com workflow operacional de transferências.
- Migration `InventoryTransfer`/items com `CHECK` para warehouses distintos e quantidade positiva.
- Create/list/detail/update draft/start/complete/cancel protegidos por `inventory:transfer` + `inventory.transfer` e feature flag.
- Conclusão transacional gera pares `TRANSFER_OUT`/`TRANSFER_IN`, atualiza balances/version, audita e publica `inventory.transfer.completed` no Outbox.
- UI `/inventory/transfers` com lista, criação multi-item e ações por estado; detalhe dedicado/drawer pode evoluir após feedback de uso.
- Coberto por testes unitários de service/controller, build API/Web, i18n e testes focados da navegação; testes concorrentes/E2E real permanecem como evidência de staging antes de produção.

**Aceite:** conclusão não produz efeito unilateral e não permite saldo negativo.

## 10.2.3 — Inventário cíclico

- **Status:** implementada nesta branch com workflow operacional de inventário cíclico.
- Migration `CycleCount`/items e snapshot de versão.
- Create/list/detail/open/count/approve/cancel protegidos por `inventory:cycle-count` + `inventory.cycle_count` e feature flag.
- Aprovação atômica com stale guard de `InventoryBalance.version`; divergências geram `ADJUSTMENT_IN/OUT` no ledger.
- UI `/inventory/cycle-counts` com lista, criação multi-SKU, abertura, contagem por item, resumo de divergência, aprovação e cancelamento.
- AuditLog, Swagger/Redoc DTOs, AsyncAPI `inventory.cycle_count.adjusted` e i18n pt-BR/en-US.

**Aceite:** contagem só altera saldo após aprovação autorizada; cada divergência possui motivo e movimento rastreável.

## Definition of Done por sprint

1. Testes RED/GREEN e regressão do módulo afetado.
2. `prisma validate`, migration test e rollback operacional documentado quando houver schema.
3. API build/lint e Web type-check/build.
4. `npm run i18n:check` e ausência de hardcode nas superfícies novas.
5. Swagger/Redoc com DTOs/exemplos/erros e autorização.
6. AsyncAPI somente para eventos realmente publicados/consumidos.
7. README, docs index, PRD/SDD/backlog/runbook/UI atualizados.
8. Logs/audit sanitizados e testes cross-tenant/RBAC/capability.

## Mercado Pago Production Readiness & 9B Preparation

Este pacote usa a `feature/10.2` como branch agregadora e uma branch filha por sprint:

```text
MP-0  — Current State Hardening
MP-1  — Automatic Token Refresh Lifecycle
MP-2  — Mercado Pago Webhook Foundation
MP-3  — Payment Status Sync
MP-4  — Cancel and Refund Support
MP-5  — Production E2E Payment Validation
MP-6  — 9B.0 Mercado Pago Financial Readiness
9B-1  — Financial Accounts and Opening Balance
9B-2  — Mercado Pago Financial Ingestion
9B-3  — Mercado Livre x Mercado Pago Matching
```

Sprints 9B-4 a 9B-7 permanecem planejadas após ingestão/matching confiáveis. Ver PRD/SDD/backlog específicos:

- `docs/prd/10.2-mercado-pago-production-readiness-9b-prd.md`
- `docs/sdd/10.2-mercado-pago-production-readiness-9b-sdd.md`
- `docs/backlog/10.2-mercado-pago-production-readiness-9b-backlog.md`
