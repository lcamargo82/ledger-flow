sequenceDiagram autonumber actor User as Usuário Tenant A participant Web as Vue Dashboard participant API as NestJS API participant Guard as Auth/Permission Guard participant Repo as Repository participant PG as PostgreSQL User->>Web: Acessa pagamento Web->>API: GET /payments/payment-id API->>Guard: Valida JWT Guard-->>API: currentUser com tenantId = Tenant A API->>Repo: findPaymentById(paymentId, tenantId) Repo->>PG: SELECT WHERE id = paymentId AND tenant_id = Tenant A alt Pagamento pertence ao Tenant A PG-->>Repo: Payment Repo-->>API: Payment API-->>Web: 200 OK Web-->>User: Exibe detalhe else Pagamento pertence a outro tenant ou não existe PG-->>Repo: null Repo-->>API: null API-->>Web: 404 NOT_FOUND ou 403 FORBIDDEN Web-->>User: Mensagem amigável end