sequenceDiagram autonumber actor User as Usuário Financeiro participant Web as Vue Dashboard participant API as NestJS API participant Redis as Redis participant PG as PostgreSQL participant Factory as PaymentGatewayFactory participant Stripe as Stripe Adapter participant StripeAPI as Stripe API participant Mongo as MongoDB participant Outbox as Outbox Event participant Rabbit as RabbitMQ participant Worker as Notification/Webhook Worker User->>Web: Preenche formulário de cobrança Web->>API: POST /payments API->>API: Valida JWT, tenant e permissão payments:create API->>Redis: Verifica idempotencyKey alt Idempotency key já processada Redis-->>API: Resultado anterior API-->>Web: Retorna cobrança existente Web-->>User: Exibe toast informativo else Nova cobrança API->>PG: Inicia transação API->>PG: Cria Payment com status PROCESSING API->>PG: Cria PaymentEvent payment.created API->>PG: Cria OutboxEvent payment.created API->>PG: Commit API->>Factory: Resolve provider do tenant Factory-->>API: StripePaymentGateway API->>Stripe: createPayment(input) Stripe->>StripeAPI: Cria payment intent/cobrança StripeAPI-->>Stripe: Resposta do gateway Stripe-->>API: GatewayPaymentResponse API->>Mongo: Salva payload bruto do Stripe API->>PG: Atualiza Payment com externalId/status API->>Redis: Salva resultado da idempotencyKey API-->>Web: 201 Created Web-->>User: Exibe toast de sucesso end Outbox->>Rabbit: Worker publica payment.created Rabbit->>Worker: Consumer processa evento Worker->>PG: Cria notificação ou webhook pendente